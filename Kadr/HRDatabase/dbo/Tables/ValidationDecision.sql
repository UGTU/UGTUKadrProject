CREATE TABLE [dbo].[ValidationDecision] (
    [ID]   INT           IDENTITY (1, 1) NOT NULL,
    [Name] VARCHAR (100) NOT NULL,
    CONSTRAINT [PK_ValidationDecision] PRIMARY KEY CLUSTERED ([ID] ASC)
);

